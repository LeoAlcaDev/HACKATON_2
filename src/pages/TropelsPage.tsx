import { Placeholder } from '../components/Placeholder';

export default function TropelsPage() {
  return (
    <Placeholder
      title="Atlas de Tropeles"
      description="Checkpoint 2: lista paginada del servidor con filtros combinables, búsqueda, orden y estado reflejado en la URL. Pendiente de implementación."
    />
  );
}
