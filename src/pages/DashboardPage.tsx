import { Placeholder } from '../components/Placeholder';

export default function DashboardPage() {
  return (
    <Placeholder
      title="Dashboard"
      description="Checkpoint 1: indicadores globales del workspace desde /dashboard/summary, con sus estados de loading, error y vacío. Pendiente de implementación."
    />
  );
}
