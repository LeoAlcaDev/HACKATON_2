import { Placeholder } from '../components/Placeholder';

export default function SectorsPage() {
  return (
    <Placeholder
      title="Sectores"
      description="Lista de sectores del workspace desde /sectors. Desde aquí se entra a la historia de cada sector (/sectors/:id/story). Pendiente de implementación."
    />
  );
}
