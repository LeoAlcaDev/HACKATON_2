import { Placeholder } from '../../components/Placeholder';

export default function SignalsFeedPage() {
  return (
    <Placeholder
      title="Feed de Señales"
      description="Checkpoint 3: infinite scroll basado en cursor, con deduplicación por id, una sola carga en vuelo, descarte de respuestas obsoletas y filtros persistidos en la URL. Pendiente de implementación."
    />
  );
}
